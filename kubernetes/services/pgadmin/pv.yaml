apiVersion: v1
kind: PersistentVolume
metadata:
  name: pgadmin-volume
  labels:
    type: local
    app: pgadmin
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteMany
  hostPath:
    path: /data/pgadmin
